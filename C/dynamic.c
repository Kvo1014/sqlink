#include <stdio.h>
#include <stdlib.h>

int *insert(  int*array, int num, int*ip, int*cap     ) 



 int main()

{       int cap=0;
        int num=0;
        int *array=0; 
        int index= 0 
  

        printf("please enter size of array:);
        scanf ("%d", &cap);
      
        array = (int*) malloc ( cap* sizeof(int));

        if (NULL==array);
          {
            printf(" os didn't gave memory.Exit....\n);
            exit (1);   
	
       
	 while(1)
        
	   {
		  printf ("please enter a number:");
		  { scanf("&d", &num }
                  

                  insert(array, num , &cap, &index )
		  
		 	 for(i=0,i<cap ,i++)
		 	  { 
		           printf("%d", &arr[i]);
		  	  }
            }
}


	insert (int *arr,int num,int*cap,int*index) 
	{ 
          int *temp=p;
    
   	 	temp=(int*) realloc( p, sizeof(int)*((*cap)*2));
		   
    		  if(temp !=NULL)
		 {
		  p=temp; 
                  (*capicity)*=2; 
		 }
                 else
                 {
                  return p; 
                 }
                 
		    
			for(i=0;i<n;i++) (*input)[i] = (float)i;
			return(n);
  	 }
   	  else return(-1);
}










