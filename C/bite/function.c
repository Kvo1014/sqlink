#include <stdio.h>
 
bitMap* CreateBitMap ( int numofFeatures)
 { 
   bitMap*bm= (bitMap*) malloc(sizeof (bitMap);
    if (!(bm))
     { 
      return NULL;
     }
   bm-> numofFeatures= numofFeatures; 
	  
         
           if(!(bm->numofFeatures))
           {
            free(bm);
            return NULL;
           }
   return bm;
 }   









int bitOn (bitmap *bm, int n)
 {












 }


 int BitOff(bitmap*bm, int n) 
 {






 }
 
 int bitStatus(bitmap*bm, int n);
 {
      
  
 
 } 







